<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <title>Home</title>
</head>
<body>
    <div class = "pl-5 pt-3">
        <h1 class = "mb-4">My Books</h1>
        <a href="/books/add" class = "btn btn-success mb-4">Add Book</a>
        <table class = "table table-striped table-responsive">
            <tr>
                <th>Title</th>
                <th>Author</th>
                <th>Category</th>
                <th>Publisher</th>
                <th>Publishing Date</th>
                <th>Operations</th>
            </tr>
            <% books.forEach(book => { %>
                <tr>
                    <td><%= book.title  %> </td>
                    <td><%= book.author %> </td>
                    <td><%= book.category %> </td>
                    <td><%= book.publisher %> </td>
                    <td><%= book.publishingDate.toLocaleDateString() %> </td>
                    <td colspan="2">
                        <a href = "/books/edit/<%= book.id %>" class = "btn btn-primary">Edit</a>
                        <form action="/books/<%= book.id %>?_method=DELETE" method = "POST" class = "d-inline">
                            <button class = "btn btn-danger" type = "submit">Delete</button>
                        </form>
                    </td>
                </tr>
            <% }); %> 
        </table>
    </div>
</body>
</html>